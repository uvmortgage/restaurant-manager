name: One-time DB Migration

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/migrate.yml'

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - name: Drop unique constraint on orders.order_date
        env:
          PGPASSWORD: "4^ZgfZY3Y7#SutjWj2EXXs77&9jZ"
        run: |
          psql "postgresql://postgres.qnpnisokvcsiysiakayr@aws-1-us-east-2.pooler.supabase.com:5432/postgres" \
            -c "ALTER TABLE ibgsc.orders DROP CONSTRAINT IF EXISTS orders_order_date_key;"
          echo "Migration complete"
